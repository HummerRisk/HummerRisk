<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.hummerrisk</groupId>
    <artifactId>hummerrisk</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>HummerRisk</name>
    <url>https://hummerrisk.com</url>
    <description>云原生安全平台</description>

    <properties>
        <hummerrisk.version>1.0.0</hummerrisk.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>1.8</java.version>
        <spring-boot.version>3.0.2</spring-boot.version>
        <spring-cloud.version>2022.0.0</spring-cloud.version>
        <spring-cloud-alibaba.version>2.2.10-RC1</spring-cloud-alibaba.version>
        <swagger.fox.version>3.0.0</swagger.fox.version>
        <tobato.version>1.27.2</tobato.version>
        <pagehelper.version>5.3.0</pagehelper.version>
        <druid.version>1.2.15</druid.version>
        <dynamic-ds.version>3.5.2</dynamic-ds.version>
        <commons.io.version>2.11.0</commons.io.version>
        <commons.fileupload.version>1.4</commons.fileupload.version>
        <fastjson.version>2.0.22</fastjson.version>
        <jjwt.version>0.9.1</jjwt.version>
        <minio.version>8.2.2</minio.version>
        <poi.version>5.2.3</poi.version>
        <transmittable-thread-local.version>2.14.2</transmittable-thread-local.version>
        <swagger.parser>1.0.61</swagger.parser>
        <knife4j.version>3.0.3</knife4j.version>
        <swagger.models>1.6.6</swagger.models>
        <swagger.annotations>2.2.2</swagger.annotations>
    </properties>

    <!-- 依赖声明 -->
    <dependencyManagement>
        <dependencies>

            <!-- SpringCloud 微服务 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- SpringCloud 微服务 -->

            <!-- SpringCloud Alibaba 微服务 -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- SpringCloud Alibaba 微服务 -->

            <!-- SpringBoot 依赖配置 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- SpringBoot 依赖配置 -->

            <!-- FastDFS 分布式文件系统 -->
            <dependency>
                <groupId>com.github.tobato</groupId>
                <artifactId>fastdfs-client</artifactId>
                <version>${tobato.version}</version>
            </dependency>
            <!-- FastDFS 分布式文件系统 -->

            <!-- Swagger 依赖配置 -->
            <dependency>
                <groupId>io.swagger</groupId>
                <artifactId>swagger-parser</artifactId>
                <version>${swagger.parser}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger</groupId>
                <artifactId>swagger-models</artifactId>
                <version>${swagger.models}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger</groupId>
                <artifactId>swagger-annotations</artifactId>
                <version>${swagger.models}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations</artifactId>
                <version>${swagger.annotations}</version>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>31.1-jre</version>
            </dependency>
            <!-- swagger 依赖配置 -->

            <!-- pagehelper 分页插件 -->
            <dependency>
                <groupId>com.github.pagehelper</groupId>
                <artifactId>pagehelper</artifactId>
                <version>${pagehelper.version}</version>
            </dependency>
            <!-- pagehelper 分页插件 -->

            <!-- io常用工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons.io.version}</version>
            </dependency>
            <!-- io常用工具类 -->

            <!-- excel工具 -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- 文件上传工具类 -->
            <dependency>
                <groupId>commons-fileupload</groupId>
                <artifactId>commons-fileupload</artifactId>
                <version>${commons.fileupload.version}</version>
            </dependency>
            <!-- 文件上传工具类 -->

            <!-- JSON 解析器和生成器 -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson.version}</version>
            </dependency>
            <!-- JSON 解析器和生成器 -->

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <!-- JWT -->

            <!-- 线程传递值 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>transmittable-thread-local</artifactId>
                <version>${transmittable-thread-local.version}</version>
            </dependency>
            <!-- 线程传递值 -->

            <!-- 核心模块 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-core</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 核心模块 -->

            <!-- 接口模块 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-swagger</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 接口模块 -->

            <!-- 安全模块 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-security</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 安全模块 -->

            <!-- 权限范围 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-datascope</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 权限范围 -->

            <!-- 多数据源 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-datasource</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 多数据源 -->

            <!-- 分布式事务 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-seata</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 分布式事务 -->

            <!-- 日志记录 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-log</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 日志记录 -->

            <!-- 缓存服务 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-common-redis</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 缓存服务 -->

            <!-- 系统接口 -->
            <dependency>
                <groupId>com.hummer</groupId>
                <artifactId>hummerrisk-api-system</artifactId>
                <version>${hummer.version}</version>
            </dependency>
            <!-- 系统接口 -->

            <!-- junit -->
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>4.12</version>
                <scope>compile</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-test</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-test</artifactId>
                <version>5.3.9</version>
                <scope>test</scope>
            </dependency>
            <!-- junit -->

            <!-- google gson -->
            <dependency>
                <groupId>com.google.code.gson</groupId>
                <artifactId>gson</artifactId>
                <version>2.9.1</version>
            </dependency>
            <!-- google gson -->

            <!-- flyway -->
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>7.15.0</version>
            </dependency>
            <!-- flyway -->

            <!-- mysql -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <scope>runtime</scope>
            </dependency>
            <!-- mysql -->

        </dependencies>
    </dependencyManagement>

    <modules>
        <module>hummer-ui</module>
        <module>backend</module>
    </modules>

    <repositories>
        <repository>
            <id>hummer</id>
            <name>Hummer</name>
            <url>https://registry.hummercloud.com/repository/maven-public/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/libs-milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>jcenter-snapshots</id>
            <name>jcenter</name>
            <url>https://jcenter.bintray.com/</url>
        </repository>
    </repositories>

</project>
